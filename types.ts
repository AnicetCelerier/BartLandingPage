export type CardCustomersProps = {
  content: string;
  name: string;
};

export type CardPartnersProps = {
  image: string;
  alt: string;
};

export type Customer = "null" | "Responsable de la vie de bureau" | "Employé";
